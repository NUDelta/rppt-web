Router.map(function() {
    this.route('cc', { path: '/' });

    // this.route('storyPage', {
    //     path: '/story/:_id',
    //     data: function() { return Stories.findOne(this.params._id); }
    // });

});